spring:
  profiles:
    active: local

  application:
    name: harness-app

---
spring:
  config:
    activate:
      on-profile: local

  datasource:
    # Oracle XE 18c/21c → service name 방식
    # Oracle XE 11g    → jdbc:oracle:thin:@localhost:1521:XE
    url: ${DB_URL:jdbc:oracle:thin:@localhost:1521/XE}
    username: ${DB_USERNAME:system}
    password: ${DB_PASSWORD:oracle}
    driver-class-name: oracle.jdbc.OracleDriver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2

  jpa:
    hibernate:
      ddl-auto: update            # Flyway 대신 Hibernate가 Oracle 테이블 자동 생성/변경
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100

  flyway:
    enabled: false                # 로컬 Oracle은 ddl-auto: update로 대체

  # Kafka 미설치 환경에서 앱 기동 실패 방지
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration

server:
  port: 8080
  # HTTP 응답에 서버 정보 노출 방지
  server-header: ""

jwt:
  secret: ${JWT_SECRET:local-dev-secret-key-must-be-at-least-256-bits-long!!}
  issuer: harness-app
  access-token-expiry-ms: 900000     # ★ 15분 (30분 → 15분으로 단축, 보안 강화)
  refresh-token-expiry-ms: 604800000 # 7일

rate-limit:
  login:
    capacity: 5
    refill-tokens: 5
    refill-seconds: 60

# ① AI Agent 설정
claude:
  api-key: ${CLAUDE_API_KEY:}            # .env.local 또는 환경변수로 주입
  model: claude-opus-4-5
  max-tokens: 2048

# ② Spring Batch 설정
spring.batch:
  job:
    enabled: false                        # 앱 시작 시 자동 실행 방지
  jdbc:
    initialize-schema: always            # Batch 메타 테이블 자동 생성 (H2용)

# ③ Kafka 설정
kafka:
  bootstrap-servers: ${KAFKA_SERVERS:localhost:9092}
  topics:
    data-collected: harness.data.collected
    analysis-requested: harness.analysis.requested
    analysis-completed: harness.analysis.completed

logging:
  level:
    com.harness: DEBUG
    org.hibernate.SQL: DEBUG
    # 토큰/비밀번호 로그 절대 출력 금지
    org.springframework.security: INFO

---
spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

  flyway:
    enabled: true

server:
  port: 8080
  server-header: ""

jwt:
  secret: ${JWT_SECRET}
  issuer: harness-app
  access-token-expiry-ms: 900000
  refresh-token-expiry-ms: 604800000

rate-limit:
  login:
    capacity: 5
    refill-tokens: 5
    refill-seconds: 60

logging:
  level:
    com.harness: INFO
    org.springframework.security: WARN
